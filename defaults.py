"""Save some values that are the same for training, validation and evaluation.
"""
DEFAULT_ARGS = {
    'seed': 0,
    'batch_size': 512,
    'num_episodes': 1e4,
    'num_epochs': 1e4,
    'val_freq': 100,
}

